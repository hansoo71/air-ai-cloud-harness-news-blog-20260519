# AGENT.md

## 편집 우선순위
1. 사용자가 제공한 원문 의미 보존
2. AIR Unit Presales 관점의 메시지 선명화
3. 모바일 가독성 및 한국어 줄바꿈 유지
4. `src/` 수정 후 `docs/`와 standalone 산출물을 동기화

## 정확성 규칙
- 외부 웹 검증 전에는 신규 사실을 추가하지 않는다.
- 출처 링크/기관명은 제공 원문 기준으로만 표현한다.
- 확인되지 않은 수치는 단정적 마케팅 카피로 과장하지 않는다.

## 자산 규칙
- 공개 배포용 자산은 `src/assets/images/`와 `docs/assets/images/` 모두에 둔다.
- 단일 HTML 전달본은 CSS/JS/이미지를 인라인 처리한다.
